apiVersion: apps/v1
kind: Deployment
metadata:
  name: xcloud-python-app
  namespace: xcloud
  labels:
    app: xcloud-python
    environment: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: xcloud-python
  template:
    metadata:
      labels:
        app: xcloud-python
    spec:
      containers:
      - name: python-app
        image: ghcr.io/pagecloudv1/xcloud-python:latest
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        - name: ENV
          value: "production"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
