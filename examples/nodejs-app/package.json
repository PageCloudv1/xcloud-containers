{
  "name": "xcloud-nodejs-example",
  "version": "1.0.0",
  "description": "Example Node.js application for xCloud containers",
  "main": "src/index.js",
  "scripts": {
    "dev": "node src/index.js",
    "start": "node dist/index.js",
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
